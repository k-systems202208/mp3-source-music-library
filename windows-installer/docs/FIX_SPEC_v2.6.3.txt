自宅音楽ライブラリ v2.6.3
不具合修正仕様
================================

対象1：起動状態確認URL
----------------------
入力が次のどちらでも、確認先を/api/healthへ統一します。

・http://127.0.0.1:8765/
・http://127.0.0.1:8765/music-library-search.html

正しい確認先：
http://127.0.0.1:8765/api/health

対象2：停止処理
----------------
停止時に未定義変数を参照しないこと。
停止後は次の状態に戻すこと。

・current_urlは空
・remote_busyはFalse
・auto_remote_setupはFalse
・auto_remote_setup_startedはFalse
・表示は「停止しました」
・開始ボタンを再び使用可能

対象3：復旧案内
----------------
旧バッチ版の「黒いサーバー画面」「start-music-library.bat」という案内を削除します。
現行の管理画面とスタートメニューを使う案内へ変更します。


対象4：発行元表記
------------------
Windowsの「インストールされているアプリ」および実行ファイル情報に表示される発行元を、次へ統一します。

k-systems202208

正式な発行元以外の表記は公開ファイルに残さないこと。

互換性
------
・library.dbの構造は変更しません。
・config.jsonの構造は変更しません。
・MP3や表記訂正データは変更しません。
・Tailscale Serve設定は変更しません。
